# preferi não fazer os comentários dessa questão por o código ser bem intuitivo.
pNum1 = int(input("Digite o primeiro número: "))
solicNum = filter(lambda x: x > 10, range(10))
print(solicNum(pNum1))
